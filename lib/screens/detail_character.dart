import 'package:flutter/material.dart';

class DetailCharacter extends StatelessWidget {
  const DetailCharacter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rick and Morty 2'),
      ),
    );
  }
}
